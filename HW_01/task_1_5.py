from math import sqrt

first_leg = 14
second_leg = 8
hypotenuse = sqrt(pow(first_leg, 2) + pow(second_leg, 2))
square = first_leg * second_leg / 2
print('Гипотенуза треугольника -', hypotenuse)
print('Площадь треугольника', square)
